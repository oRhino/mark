
## 错误处理

1. 用throw抛出一个错误,如果不对错误进行处理,程序会停止向下执行.
2. 错误的类型可以使用Error(包含程序的调用栈).<message,stack>,也可以自定义类型
3. 常用Error的子类 RangeError(越界) TypeError(类型) SyntaxError(语法)
4. 错误的处理: ① try-catch,try-catch-finally ②不处理, 异常会进一步的抛出, 直到最顶层的调用,如果在最顶层也没有对这个异常进行处理, 那么程序就会终止执行, 并且报错
